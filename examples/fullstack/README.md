# Full-Stack Example
## Frontend + Backend + Database layout

This example includes:
- `apps/frontend` (UI)
- `apps/backend` (API and data)
- `db/` (migration placeholders)

Governance is configured in `/.repo/`.

