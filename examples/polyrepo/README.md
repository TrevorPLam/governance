# Polyrepo Example
## Multiple related repositories with shared governance

This example shows two related repositories:
- `service-a` (core API)
- `service-b` (worker service)

Each service has its own `.repo` folder and CI workflow.

