# Metrics Collector

This tool defines governance metrics and provides the schema used by the CLI for validation and reporting.

## Files
- `metrics.schema.json`: JSON schema for metrics output

## Usage
Metrics are collected via:
```bash
governance metrics collect
```
