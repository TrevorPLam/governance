# Migration Tooling

Provides migration helpers for governance framework upgrades.

## CLI
```bash
governance migrate --dry-run
governance migrate
```

## Scope
Current migrations ensure new folders such as `.repo/metrics/`, `.repo/hitl/`, `.repo/waivers/`, and `.repo/audit/` exist.
